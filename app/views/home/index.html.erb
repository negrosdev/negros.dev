<section class="section">
  <div class="container">
    <h1 class="title mb-6 has-text-white has-text-centered">
      💕 De
      <span class="has-text-rose"> pessoas negras devs</span>
      para o mundo! 💕
    </h1>
    <div class="columns is-multiline cards-height-match mt-6">
      <% @courses.each do | course | %>
        <div class="column is-3-desktop is-4-tablet is-12-mobile mb-6">
          <%= render partial: 'components/course', locals: { course: course, effect: true } %>
        </div>
      <% end %>
    </div>
  </div>
</section>