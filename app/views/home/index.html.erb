<%= content_for :title do %> 💕 Aprenda desenvolvimento 100% Grátis  💕 <% end %>

<section class="section">
  <div class="container">
    <h1 class="title mb-6 has-text-white has-text-centered">
      💕 De
      <span class="has-text-rose"> pessoas negras devs</span>
      para o mundo! 💕
    </h1>
    <div class="columns is-multiline cards-height-match mt-6">
      <% @contents.each do | content | %>
        <div class="column is-3-desktop is-6-tablet is-12-mobile mb-6">
          <%= render partial: 'components/content', locals: { content: content, effect: true } %>
        </div>
      <% end %>
    </div>
  </div>
</section>